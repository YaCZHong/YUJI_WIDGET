<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.YUJI_WIDGET_NO_ACTIONBAR" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="shapeAppearanceLargeComponent">@style/MyShapeAppearanceLargeComponent</item>
    </style>

    <style name="MainActivityTheme" parent="Theme.YUJI_WIDGET_NO_ACTIONBAR">
        <item name="colorPrimaryDark">@color/transparent</item>
    </style>

    <!--CollapsingToolbarLayout折叠时标题文字样式-->
    <style name="CollapsedTitleTextStyle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/colorTextDark</item>
    </style>

    <style name="ExpandedTitleTextStyle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/colorTextDark</item>
    </style>

    <!--loading弹窗样式-->
    <style name="LoadingDialogStyle" parent="android:Theme.Holo.Light.Dialog">
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置dialog的背景-->
        <item name="android:background">@android:color/transparent</item>
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--如果背景需要模糊，模糊到什么程度，当值为1 时，除了dialog以外的部分全黑-->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <style name="MyShapeAppearanceLargeComponent" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

</resources>